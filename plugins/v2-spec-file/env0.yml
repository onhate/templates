version: 2
deploy:
  steps:
    terraformPlan:
      after:
        - echo Running string command
        - name: Command from step.run
          run: |
            echo Running commad from step.run
            echo Running another command from step.run
            echo "### Summary" >> $ENV0_STEP_SUMMARY
            echo "" >> $ENV0_STEP_SUMMARY
            echo "- it's cool" >> $ENV0_STEP_SUMMARY