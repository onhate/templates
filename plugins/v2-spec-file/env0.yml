version: 2
deploy:
  steps:
    terraformPlan:
      after:
        - echo Running string command
        - name: Command from step.run
          run: |
            echo Running command from step.run
            echo Running another command from step.run
            printf "Check failed" >> $ENV0_STEP_SUMMARY 
            printf "### Summary\n\n" >> $ENV0_STEP_CONTENT
            printf "- it's cool" >> $ENV0_STEP_CONTENT